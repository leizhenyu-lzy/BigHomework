cmake_minimum_required(VERSION 3.0)
PROJECT(OpenCV4)

SET(SRC_LIST HarrisCorner.cpp)


MESSAGE(STATUS "BINARY DIR" ${PROJECT_BINARY_DIR})
MESSAGE(STATUS "SOURCE DIR" ${PROJECT_SOURCE_DIR})

ADD_EXECUTABLE(harris ${SRC_LIST})